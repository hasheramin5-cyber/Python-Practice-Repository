# A Program to create a function with one parameter.

def greet(name):
    print(f"Hello, {name}!")

greet("Hasher")

# Explanation:
# The program defines a function that accepts one parameter named 'name'. When the function is called, a value is passed as an argument, and that value is stored in the parameter. The function then uses the parameter to display a personalized greeting.
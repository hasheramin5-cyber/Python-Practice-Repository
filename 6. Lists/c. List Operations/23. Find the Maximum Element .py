# A Program to find the largest element in a list.

numbers = [45, 12, 89, 23, 67]
print(f"\nList: {numbers}")

print("Largest Element is:", max(numbers))

# Explanation:
# The program uses Python's built-in max() function to find the largest element in the list. The function compares all elements and returns the highest value.
# A Program to find the length of a string.

text = input("\nEnter a string: ")

length = len(text)

print(f"Length of '{text}' is {length} letter.")

# Explanation:
# The program asks the user to enter a string and calculates its length using Python's built-in len() function. The length represents the total number of characters in the string, including spaces and punctuation marks.
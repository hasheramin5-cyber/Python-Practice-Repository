# A Program to multiply two numbers using a function.

def multiply(number1, number2):
    print("Product =", number1 * number2)

multiply(7, 6)

# Explanation:
# The program defines a function that takes two numbers as parameters and multiplies them together. Whenever the function is called with different arguments, it calculates and displays the product of those numbers.
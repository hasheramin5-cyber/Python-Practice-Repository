# A Program to convert a string to uppercase.

text = input("\nEnter a string: ")

print("Uppercase String =", text.upper())

# Explanation:
# The program asks the user to enter a string and converts all lowercase letters into uppercase using the upper() method. Characters that are already uppercase or are not alphabetic remain unchanged.
# A Program to access the first character of a string.

text = input("\nEnter a string: ")

print("First Character is:", text[0])

# Explanation:
# The program accesses the first character of a string using index 0. In Python, string indexing starts from 0, meaning the first character is always located at position 0.
# A Program to repeat a string multiple times.

text = input("\nEnter a string: ")

print(text * 3)

# Explanation:
# The program repeats a string using the multiplication (*) operator. Multiplying a string by an integer creates multiple copies of the string. In this example, the entered string is printed three times consecutively.
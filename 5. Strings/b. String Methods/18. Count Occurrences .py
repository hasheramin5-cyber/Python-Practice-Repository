# A Program to count the occurrences of a character or word in a string.

text = input("\nEnter a string: ")

search = input("Enter a character or word to count: ")

print("Occurrences =", text.count(search))

# Explanation:
# The program counts how many times a specified character or word appears in the string using the count() method. The total number of occurrences is then displayed.
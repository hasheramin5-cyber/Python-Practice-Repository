# A Program to join a list of words into a single string.

words = ["Python", "is", "Awesome", "!!"]

sentence = " ".join(words)

print(sentence)

# Explanation:
# The program uses the join() method to combine the elements of a list into a single string. The string before the join() method determines the separator placed between each element. In this example, a space separates the words.
# A Program to find the smallest element in a list.

numbers = [45, 12, 89, 23, 67]

print("\nList:", numbers)

print("Smallest Element is:", min(numbers))

# Explanation:
# The program uses Python's built-in min() function to find the smallest element in the list. The function compares all elements and returns the lowest value.
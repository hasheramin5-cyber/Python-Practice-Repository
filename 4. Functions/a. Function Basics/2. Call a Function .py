# A Program to demonstrate how to call a function.

def message():
    print("\nWelcome to Python Functions!")

message()

# Explanation:
# The program defines a function named 'message' and then calls it. When the function is called, Python executes all the statements inside the function. This example demonstrates that defining a function stores it in memory, while calling the function actually runs its code.
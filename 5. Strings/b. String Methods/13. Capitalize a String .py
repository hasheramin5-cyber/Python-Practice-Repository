# A Program to capitalize the first letter of a string.

text = input("\nEnter a string: ")

print("Capitalized String =", text.capitalize())

# Explanation:
# The program uses the capitalize() method to convert the first character of the string into uppercase while changing all remaining alphabetic characters to lowercase. This method is useful for formatting sentences.
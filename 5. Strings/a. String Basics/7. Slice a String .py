# A Program to slice a string.

text = input("\nEnter a string: ")

print("Slice =", text[0:5])

# Explanation:
# The program demonstrates string slicing by extracting a portion of the string. The slice [0:5] returns characters starting from index 0 up to, but not including, index 5. Slicing is commonly used to extract specific parts of a string.
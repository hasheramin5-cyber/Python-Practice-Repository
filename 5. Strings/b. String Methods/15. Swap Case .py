# A Program to swap the letter case of a string.

text = input("\nEnter a string: ")

print("Swapped String =", text.swapcase())

# Explanation:
# The program changes every uppercase letter to lowercase and every lowercase letter to uppercase using the swapcase() method. This method reverses the case of all alphabetic characters in the string.
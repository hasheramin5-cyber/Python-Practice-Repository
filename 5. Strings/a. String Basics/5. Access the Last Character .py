# A Program to access the last character of a string.

text = input("\nEnter a string: ")

print("Last Character is:", text[-1])

# Explanation:
# The program accesses the last character of a string using negative indexing. In Python, the index -1 always refers to the last character of a string, making it an easy way to access the end of the string.
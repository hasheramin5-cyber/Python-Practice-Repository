# A Program to find the length of a list.

languages = ["Python", "Java", "C++", "JavaScript", "Go"]

print("\nList:",languages)
print("Length of List is:", len(languages))

# Explanation:
# The program uses Python's built-in len() function to determine the total number of elements in the list. The len() function returns the size of the list regardless of the data type stored in it.
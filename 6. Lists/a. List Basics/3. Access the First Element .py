# A Program to access the first element of a list.

numbers = [10, 20, 30, 40, 50]

print("First Element =", numbers[0])

# Explanation:
# The program accesses the first element of the list using index 0. In Python, list indexing starts from 0, so the first element is always located at index position 0.